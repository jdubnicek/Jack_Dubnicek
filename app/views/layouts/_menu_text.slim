.btn.btn-link = "#{current_order.menu_dishes.map(&:quantity).reduce(0, :+)} items selected. Your sub-total is #{number_to_currency current_order.subtotal}"
= link_to "Edit Order", menu_path, class: "btn btn-link"
